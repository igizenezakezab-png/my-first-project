
//loop 
function discount(){
    let iterms = [500,1000,3000,20000];
    for(let i = 0; i < iterms.length;i++){
        
        if (iterms[1] > 600){
            console.log(iterm[i] + "discount applied");

        }else{
            console.log(iterms[i] + "discount denied");
            
        }
    }
}